---
- name: Install Maven
  hosts: 10.0.0.5
  become: true  # This is necessary if the installation requires elevated privileges

  tasks:
    - name: Update apt package index
      apt:
        update_cache: yes

    - name: Install Maven
      apt:
        name: maven
        state: present

